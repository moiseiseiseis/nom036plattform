SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Conexión a Postgres (esquema nom036) vía el connection pooler de Supabase
# (puerto 6543). La conexión directa solo resuelve a IPv6 y Railway no tiene
# salida IPv6.
DATABASE_URL=
