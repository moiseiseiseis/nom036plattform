# Python
service/.venv/
service/__pycache__/
service/**/__pycache__/
service/.env
*.pyc

# OS
.DS_Store
Thumbs.db

# Claude Code (config local de máquina, no del proyecto)
.claude/

# Informes .docx generados (pueden contener datos de empresas reales) —
# quedan solo en disco local, nunca en el historial de git.
contenido/*.docx
